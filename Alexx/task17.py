# Фільтр дублікатів
#
# Умова: Очисти список [1, 2, 2, 3, 4, 4, 4, 5] від повторюваних елементів, щоб залишилися тільки
# унікальні числа.
# Підказка: Передай цей список у функцію set(). Множини автоматично видаляють усі дублікати.

def myfunc():
    mylist = [1, 2, 2, 3, 4, 4, 4, 5]
    mylist = set(mylist)
    print(mylist)# 111111111111

myfunc()